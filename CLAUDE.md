# プロジェクトの目的・構想

## 目的
初心者のためのテトリスの最善手を提示するソフトの開発。

## 構想
- ネクスト・ホールド・現状の地形を鑑みた最善手をドットで表示し、人間が操作する。
- Switchなどのゲーム機からキャプチャボードでPC上にテトリスの画面を表示する。
  そのため、正確かつ素早い画像認識が求められる。
- 最善手は、相手からの妨害で段がせりあがる場合を除き、表示を切り替えない。
- 最善手を切り替えるタイミングは、ネクスト・ネクネク(2手先)・ホールドに
  あるテトリミノが変化した時とする。
- 思考AIはCold Clear(Cold Clear 2)を用いる。

# このプロジェクトでの作業ルール

不具合修正・機能追加を行う際は、必ず以下の5ステップを順番に踏むこと。
途中を省略したり、順序を入れ替えたり、いきなり修正に飛びついたりしない。

## ステップ1: 情報を集めて状況を整理する
- エラーメッセージ・スタックトレース・`debug_log.txt`・`debug_frames_history/`の
  生画像・`debug_capture.mp4`など、関連する情報を最初に揃える。
- 関連するコードパスだけを読む。無関係なファイルは触らない。

## ステップ2: 原因の仮説を優先度つきで出す
- いきなり修正せず、考えられる原因を可能性が高い順に列挙する。
- 仮説が浅いと感じたら5Whysで深掘りする(なぜそうなるのか、を何度も
  自問する。必要ならユーザーに「なぜ」を問いかけてもらう)。

## ステップ3: 証拠を集めて仮説を検証する
- 再現する失敗・生画像・ログの該当箇所など、実際の証拠を集めて
  どの仮説が正しいかを絞り込む。証拠なしに仮説を確定させない。

## ステップ4: 根本原因を1文で確定する
- 原因がぼやけたまま修正すると直らないままになるため、
  「〜が原因で〜が起きている」と1文で言い切れる根本原因を提示する。
- ここでユーザーに提示し、次に進んでよいか確認する
  (承認するかどうかはユーザーが決める)。

## ステップ5: 最小限の修正とテストで仕上げる
- 確定した根本原因に対してのみ、必要十分な最小限の変更を行う。
  ついでの周辺リファクタリングや関係のない改善を混ぜない。
- 変更に対応する回帰テストを追加し、既存テストと合わせて実行する
  (`./venv/Scripts/python.exe -m unittest discover -s tests`)。
- 実機でしか検証できない変更(画面キャプチャ・オーバーレイ描画等)は、
  その旨を明記し、ユーザーに実機確認を依頼する。
